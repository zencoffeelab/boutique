import { z } from "zod";
import type { ActionFunctionArgs, LoaderFunctionArgs, MetaFunction } from "react-router";
import { Form, Link, useActionData, useLoaderData } from "react-router";
import { Logo } from "~/components/logo";
import { requireAdmin } from "~/lib/auth.server";
import { createServiceSupabase } from "~/lib/supabase.server";

const pageSchema = z.object({ intent: z.literal("save_page"), pageKey: z.string().regex(/^[a-z0-9-]+$/), status: z.enum(["draft", "published", "archived"]), titleFr: z.string().min(2), titleEn: z.string().min(2), seoTitleFr: z.string().min(2), seoTitleEn: z.string().min(2), seoDescriptionFr: z.string().min(10), seoDescriptionEn: z.string().min(10), contentFr: z.string().min(10), contentEn: z.string().min(10) });
const defaults = ["accueil", "a-propos", "professionnel", "faq", "contact", "cgv", "mentions-legales", "politique-de-confidentialite"];

export async function loader({ request }: LoaderFunctionArgs) {
  const admin = await requireAdmin(request); if (admin.demo) return { demo: true, pages: [] };
  const client = createServiceSupabase(); if (!client) throw new Response("Database unavailable.", { status: 503 }); const { data, error } = await client.from("content_pages").select("*,content_page_translations(*)").order("page_key"); if (error) throw new Response(error.message, { status: 500 }); return { demo: false, pages: data ?? [] };
}
export async function action({ request }: ActionFunctionArgs) {
  const admin = await requireAdmin(request); if (admin.demo) return { ok: false, message: "Lecture seule en démonstration." }; const parsed = pageSchema.safeParse(Object.fromEntries(await request.formData())); if (!parsed.success) return { ok: false, message: "Les contenus français et anglais sont requis." };
  if (parsed.data.status === "published" && /(?:à compléter|to complete)/i.test(`${parsed.data.seoDescriptionFr} ${parsed.data.seoDescriptionEn} ${parsed.data.contentFr} ${parsed.data.contentEn}`)) return { ok: false, message: "Remplacez tous les textes provisoires avant publication." };
  const client = createServiceSupabase(); if (!client) return { ok: false, message: "Base indisponible." }; const { data: page, error } = await client.from("content_pages").upsert({ page_key: parsed.data.pageKey, status: parsed.data.status, updated_at: new Date().toISOString() }, { onConflict: "page_key" }).select("id").single(); if (error || !page) return { ok: false, message: error?.message ?? "Page non créée." };
  const blocks = (text: string) => text.split(/\n{2,}/).map((content) => content.trim()).filter(Boolean).map((content) => ({ type: "paragraph", content }));
  const translations = [{ locale: "fr-FR", title: parsed.data.titleFr, seo_title: parsed.data.seoTitleFr, seo_description: parsed.data.seoDescriptionFr, blocks: blocks(parsed.data.contentFr) }, { locale: "en-GB", title: parsed.data.titleEn, seo_title: parsed.data.seoTitleEn, seo_description: parsed.data.seoDescriptionEn, blocks: blocks(parsed.data.contentEn) }].map((translation) => ({ ...translation, page_id: page.id })); const { error: translationError } = await client.from("content_page_translations").upsert(translations, { onConflict: "page_id,locale" }); if (translationError) return { ok: false, message: translationError.message };
  await client.from("audit_log").insert({ actor_id: admin.id, action: "content_page.updated", entity_type: "content_page", entity_id: page.id, after_data: parsed.data }); return { ok: true, message: "Page enregistrée." };
}
export const meta: MetaFunction = () => [{ title: "Contenus | Zen Coffee Lab" }, { name: "robots", content: "noindex,nofollow" }];

export default function AdminContent() {
  const { demo, pages } = useLoaderData<typeof loader>(); const result = useActionData<typeof action>(); const byKey = new Map(pages.map((page) => [page.page_key, page])); const keys = [...new Set([...defaults, ...byKey.keys()])];
  return <div className="admin-shell"><aside className="admin-sidebar"><Logo /><nav><Link to="/admin">Tableau de bord</Link><Link to="/admin/commandes">Commandes</Link><Link aria-current="page" to="/admin/contenus">Contenus</Link></nav></aside><main className="admin-main"><header className="admin-heading"><div><p className="eyebrow">Mini-CMS</p><h1>Contenus</h1></div></header>{demo ? <p className="admin-notice">Connectez Supabase pour éditer les pages. Le CMS accepte uniquement des blocs de paragraphes structurés.</p> : null}{result?.message ? <p className={result.ok ? "form-message" : "form-message form-error"}>{result.message}</p> : null}<div className="admin-content-list">{keys.map((key) => { const page = byKey.get(key); const fr = page?.content_page_translations?.find((translation: any) => translation.locale === "fr-FR"); const en = page?.content_page_translations?.find((translation: any) => translation.locale === "en-GB"); const text = (translation: any) => (translation?.blocks ?? []).map((block: any) => block.content).join("\n\n"); return <details className="ui-card admin-content-page" key={key}><summary><strong>{key}</strong><span className="ui-badge">{page?.status ?? "draft"}</span></summary><Form method="post"><input type="hidden" name="intent" value="save_page" /><input type="hidden" name="pageKey" value={key} /><div className="field"><label>Statut<select name="status" defaultValue={page?.status ?? "draft"}><option value="draft">Brouillon</option><option value="published">Publié</option><option value="archived">Archivé</option></select></label></div><div className="admin-content-columns"><fieldset><legend>Français</legend><div className="field"><label>Titre<input name="titleFr" defaultValue={fr?.title ?? key} required /></label></div><div className="field"><label>Titre SEO<input name="seoTitleFr" defaultValue={fr?.seo_title ?? key} required /></label></div><div className="field"><label>Description SEO<textarea name="seoDescriptionFr" defaultValue={fr?.seo_description ?? "Description à compléter avant publication."} required /></label></div><div className="field"><label>Paragraphes<textarea name="contentFr" defaultValue={text(fr) || "Contenu à compléter avant publication."} required /></label></div></fieldset><fieldset><legend>English</legend><div className="field"><label>Title<input name="titleEn" defaultValue={en?.title ?? key} required /></label></div><div className="field"><label>SEO title<input name="seoTitleEn" defaultValue={en?.seo_title ?? key} required /></label></div><div className="field"><label>SEO description<textarea name="seoDescriptionEn" defaultValue={en?.seo_description ?? "Description to complete before publication."} required /></label></div><div className="field"><label>Paragraphs<textarea name="contentEn" defaultValue={text(en) || "Content to complete before publication."} required /></label></div></fieldset></div><button className="ui-button ui-button--default" type="submit" disabled={demo}>Enregistrer</button></Form></details>; })}</div></main></div>;
}
